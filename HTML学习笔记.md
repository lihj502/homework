# vscode学习笔记
* [vscode学习笔记](#vscode学习笔记)  
      * [1.1 前期准备](#11-前期准备)
   * [HTML学习笔记](#html学习笔记)
      * [一、对HTML所制作的产品——网站、网页初认识](#一对html所制作的产品网站网页初认识)
      * [二、读取HTML网页的工具——浏览器内核](#二读取html网页的工具浏览器内核)
      * [三、HTML学习——正文](#三html学习正文)
         * [3.1 对HTML的初印象](#31-对html的初印象)
         * [3.2 HTML之标签](#32-html之标签)
            * [3.2.1 标签总述](#321-标签总述)
            * [3.2.2 标签的简单分类](#322-标签的简单分类)
            * [3.2.3 标签之间的关系](#323-标签之间的关系)
            * [3.2.3 html标签学习过程记录](#323-html标签学习过程记录)
         * [四、HTML之块级元素](#四html之块级元素)
         * [五、HTML之行内元素](#五html之行内元素)
   * [参考资料](#参考资料)



**在使用时一定要注意，在vscode中的所有重要标记符都要用英文的方式写，中文无法正常识别**。

使用英文输入`!`即可创建一套完整标签构架

### 1.1 前期准备

通过ctrl+shift+p打开相关设置功能菜单，选择Configure Display Language，接着选择Install additional languages，此时，在左侧会弹出相关的语言插件，我们选择中文简体，点击Install

**PS:** 不要点change languages，因为没有下载中文包，因此没有办法直接change

**推荐安装插件:**

1. open in Browser可以进行效果浏览

2. Auto Rename Tag插件自动重命名配对的HTML标签

3. Auto Close Tag插件自动闭合标签

4. Live Server插件，每次更改保存后自动刷新网页浏览界面，显示每次更改右击HTML代码界面，选择open with Live Server

   <img src="https://gitee.com/gritandsea/picture/raw/master/img/20210117115138.png" style="zoom:50%;" />

4. Highlight Matching插件，将当前选中的标签以及与之相匹配的标签使用高亮标记
5. Image preview插件，无论是HTML中的img路径图片链接还是css背景图片的链接，都可以在代码的最左边区域显示这个图片缩小后的样子，如果光标浮于链接上方，那么就会出现一个大的预览图片



# HTML学习笔记

## 一、对HTML所制作的产品——网站、网页初认识

**首先，什么是网站呢？**

网站就可以理解为在遵守因特网的规则下，使用HTML等制作的网页集合。

**那么什么是网页呢？**

网页最通俗的理解就是网站的一“页”，我通过对HTML的学习才能够理解，一个网页就是一个HTML格式（大多以.html结尾）的文件，而它的识别则需要通过编译器“浏览器“来实现（就是不知道这么说是不是正确）。



## 二、读取HTML网页的工具——浏览器内核

浏览器内核又被称为**渲染引擎**，它的功能主要是负责读取网页内容，整理讯息，并最终显示页面。



## 三、HTML学习——正文

### 3.1 对HTML的初印象

HTML又叫做”超文本标记语言“，它可以通过编写代码来对网页进行描述。

千万不要看到语言就想到的是编程语言，HTML是一种标记语言

**标记语言:** 包含的是一套标记标签，例如网页上的文字显示需要文字标签，网页上的图片显示需要图片标签，还有音频标签等等。

因为HTML可以加入图片、声音、动画等内容，它的内容超越了文本的限制 ，所以HTML被叫做”超文本语言“。

它被称为“超文本语言”的另一个原因是它可以从一个文件中跳转到另一个文件中，与网络上世界各地的文件相连接，而这一特点又被称为“超级链接文本”，所以同样简称为“超文本”。



### 3.2 HTML之标签

#### 3.2.1 标签总述

标签可以说是html的核心所在，它是由一对尖括号包裹的一小段具有特定含义可以被浏览器识别的单词所构成（例如<html>）

**注意:** 虽然标签对于大小写<html> 和 <HTML>并不敏感，但是因为在万维网联盟的HML4的推荐标准中推荐是用小写，同时新版本的XHTML也被要求使用小写属性，因此我决定还是规规矩矩的使用小写输入。

**标签分为两部分:** 一部分是开始标签<html> ，另一部分是结束标签</html>, 处于两个标签之间的部分被称作“标签体”。

**注意:** 虽然在练习中我发现即使忘记了加入结束标签，我的浏览器依然可以正常显示我的内容，但是好的习惯是很重要的，因为我在网上搜索这种情况的时候被警告，如果忘记使用结束标签可能会产生不可预料的结果或错误，因此还是要保持一个好的习惯（加上结束标签）才行。



#### 3.2.2 标签的简单分类

* 双标签（`<html> </html>`）

* 单标签（`<br />`)



#### 3.2.3 标签之间的关系

* 包含关系（也有称作“嵌套关系”的）

* 并列关系

  **tip:** 如果两个标签之间的关系是包含关系，被包含元素最好缩进一个tab键。如果是并列关系，推荐上下对齐。

  **PS:** 养成好习惯



#### 3.2.3 html标签学习过程记录

1. **vs code 尝试学习使用**

2. **学习HTML的基本结构**

   **tip:** `<head> </head>`中的`<title>我创建的第一个网页</title>`出现在   网页标题中，而并非是网页内容中。

3. **学习声明标签（`<!DOCTYPE html>`）**

   此标签的作用是使浏览器可以明白使用哪个HTML版本显示网页

   本标签的意思是指使网页采取HTML5版本来显示网页，不属于HTML标签，是文档类型声明标签

4. **lang是定义文档显示的语言**

   - en定义语言为英语

   - zh-CN定义语言为中文

   但是在我在实际尝试中，无论是en，zh-CN，更或者尝试了fr（法文）都可以正常显示文档的文字

5. **` <meta charset="UTF-8" />`是一个世界通用的字符集编码库，使得计算机可以识别各种文字，并将其输出。**

6. **标题标签（类比markdown语法的标题语法规则）**<h1>

7. **段落标签**<p>

   它是块级元素，在代码行中按空格或者回车都无法在真正的网页界面展示所需的回车，必须使用标签

8. **换行标签**<br />

9. **文本格式化（将文字设置为粗体、斜体或下划线）标签**

   - 粗体`<strong></strong>`或者`<b></b>`

   - 倾斜`<em></em>`或者`<i></i>`

   - 删除线`<del></del>`或者`<s></s>`

     **PS:** del即delet，这样便于记忆

   - 下划线`<ins></ins>`或者`<u></u>`

     **PS:** ins即insert，这样便于记忆

10. **布局使用的“盒子”标签学完布局来补充**

    - `<div></div>`自己独占一行，是一个块级元素

    - `<span></span>`不独占一行

11. **图像标签及其路径**

    - 图像标签`<img src="xxx"/>`

      src是定义图像的属性（也就是属于图像的特性）

      **如果图片上面需要一段介绍文字的话可以有以下方法：**

      1. 使用`<br />`换行
      2. 使用`<h4></h4>`来强制使文字成为标题
      3. 使用`<p></p>`
      4. 使用`<div></div>`
      5. 使用`<span></span>`

      **图像的各种属性**：

      1. src——这是必须的，显示图像的链接路径

      2. alt——在图像无法加载，显示失败的时候，将会出现文字在本应出现图像的地方替换加载失败的图片

      3. title——当光标移动到图片上方时，将会浮现的文字（可以成为对图片的介绍）

      4. width（hight）——给图片设置宽度（高度）

         **PS:** 但是一般对图像的修改都会采用只修改高度（宽度），因为图像会默认将另一个属性同时等比例缩放，如果两个属性同时更改可能因为无法精确掌握图片默认的比例而造成图片失真。

      5. border——为图像的边框设置粗细，但是因为还没有学习css，所以没有办法进行更加高级的美化。

12. **目录文件夹和根目录**

- **目录文件夹:** 对于存放了我制作网页的所有相关素材的文件夹的专有名词

- **根目录:** 目录文件夹打开后的第一层即被称为根目录

  学会用vccode对文件进行管理

13. **路径**

   * **相对路径**

     只需要通过特定的符号来表示目标图片与HTML文件的相对位置即可展示，共分为以下三种情况：

     1. 和HTML文件同一级路径——不需要任何特殊符号，直接粘贴文件名

     2. 在HTML文件下一级路径——需要`/`来表达

        首先粘贴和HTML文件在同一级的目标图片所在的文件夹名称，再使用`/`粘贴图片名称即可（如果图片还在更下一级，就继续重复最初的操作即可）

     3. 在HTML文件的上一级路径——需要`.../`来表示

        但是事实上我觉得这个应该不会常用，一般我都可能会把HTML文件放在根目录里面，应该不会出现出现上一级路径的情况

     **总结：总之这种表达方式比直接从网上复制地址要简洁美观，而且对图片的位置也很明显。**

   * **绝对路径**

     最通俗的解释是目标图片在本地电脑中的位置（此方法的局限性在于一旦将HTML文件发送给其他人，别人没有与你相同的目标图片所处位置的话将会无法显示图片）或者是完整的在网络上的地址（此方法的缺点是可能目标图片的网络地址很长，使得代码可读性减弱）

     **PS:** 如果手打网络地址的话一定要注意，网络地址的斜杠方向和本地文件的斜杠方向不同

14. **超链接标签（将一个页面链接到另一个页面）**

   **对于超链接语法中的每个属性的作用的记录:**

   `<a href="链接目标的地址" target="链接地址的窗口的打开方式(又称为属性)">描述性的文字或者图像等都可以</a>`

   **记忆的方法:** 对于各种缩写，尽量去思考查阅它的完全版单词，这样通过英语和HTML语法搭建起桥梁有助于对于各种不同的缩写的理解性记忆（例如，超链接中的a，就是anchor（锚，也就是链接）的缩写）

   **对于targrt中有两种页面的打开方式可供选择:**

      1. `-self`是在原页面直接打开超链接内容（同样也是不写target时的默认超链接打开方式）
      2. `-blank`是自动在一个新的标签页中打开超链接内容

   **超链接标签的分类:**

   1. **外部链接**

      即链接的文件地址存在于互联网上

   2. **内部链接**

      链接的文件和本HTML文件存在在一个目标文件夹中，因此链接的地址只需要输入将被链接的文件名称即可

   3. **空链接**

      它的目的是在链接地址所在的网页尚未完善时，代替的假链接，在地址位置填入“#”即可

   4. **下载链接**

      如果链接的地址是一个文件或者压缩包的话，就会自动下载这个文件

当然，链接的地址可以是多样的，那么同样的，可设置超链接的对象也是多元化的，不仅仅是文字可以设置超链接，图像、表格、视频等都可以实现超链接。

5. **锚点链接**

   通俗理解就是在当前页面中可以跳转至指定的当前页面位置的一个链接。

   锚点链接的创建方式如下：

   1. 找到将要跳转的目标地点，在这个地点的标题处添加一个id属性（即`<h3 id="xxx">``<h3>`）

      **PS：**xxx是为这个id属性起的名字，可以随意起

   2. 在超链接文本的地址处用“#xxx”来代替本应写的地址即可。

15. **注释标签**

   类比c语言中注释，只是标志不同，但是同样都是不会被显示出来。

   它的符号为`<!--xxx-->`（当然操作起来依然有点麻烦，因此有快捷键ctrl+/代替）。

   因为一个网页中的代码数量很庞大，因此有一个良好的写注释的习惯有利于快速明白自己每大一段代码的目的。

16. **特殊字符**

    此部分主要是介绍了一大堆无法通过直接输入在HTML页面上显示的文字

17. **表格标签**

   * 表格相关属性

     在学习教程的过程中，有提示表格标签中的一些对于居中对齐、边框设置等属性在实际开发中是不常用的，在未来会通过css来设置，因此在学习这部分时，需要记忆的重点是这些属性对应的英语单词（因为在未来的css中要用）

   * 表格结构标签（方便整理表格 ，使表格的结构更加清晰）

   * 合并单元格

     - 跨行合并rowspan="所需合并单元格的个数"

     - 跨列合并colspan="所需合并单元格的个数"

       在做跨行（跨列）合并时，写合并代码的单元格被称为目标单元格，跨行（跨列）合并时，目标单元格为将要合并的最上侧（最左侧）单元格，将合并代码输入在`<td>`中

       例如`<td colspan="x">`

       最后不要忘记在代码中删除初目标单元格以外多余的将要合并的单元格。（我认为这种方式这种描述不是特别的直观，可以把合并单元格这种称呼改为扩大单元格，这样只需要描述为在需要扩大的单元格的<td>中添加代码，并输入所要扩大的格数就可以完成对单元格的扩大了，我认为可能这样更加直观一些）

18. **列表标签**

   * **无序列表**

     虽然教程中提示，在`<ul></ul>`中只能在`<li></li>`中输入，不可以在外面输入

     但是我在实际尝试中发现，即使在`<ul></ul>`中输入文字依然可以正常的显示，只是没有前面的小圆点，不过可以正常的添加其他的标签（比如换行等等），效果相当于在`<ul></ul>`外面输入文字一样。

     `<li></li>`中可以放置任何元素

   * **有序列表**

     除了需要用的`<ol></ol>`有区别以外，其余的全部相同

     效果上即出现顺序数字

   * **自定义列表**

     它的格式为

     ```html
     <dl>
     	<dt></dt>
     
     	<dd></dd>
     
     	<dd></dd>
     	
     	 ......
     
     </dl>
     ```

     **我的理解是：大标题+子标题+子标题+......** 

19. **表单标签**

   * **表单域**

     用来包含各种表单元素，并将其包含范围内的表单元素提交给服务器

     其中`<form></form>`标签来实现表单域

     **在form中常用的属性:**

     **PS:** 很多知识还没有学习，目前只能了解一下

     * action，用来指定接收处理表单数据的服务器程序的url地址

       url地址： 就是一个给定的在 Web 上的地址。正常来说，每个有效的 URL 都指向一个唯一的资源。这个  资源可以是一个 HTML 页面，一个 CSS 文档，一幅图像，等等。

       **PS：**这一部分涉及到后台服务器的编程，目前还无法实现

     * method，有post和get，在百度上搜索以后，发现涉及到后面编写服务端的知识，因此未再深入学习

     * name,用来区分表单的一个名字（最好有实际意义）

   * **表单控件（表单元素）**

     * **input输入表单元素**

       input是一个单标签，它的type属性是必须要填的，它还有很多其他的属性。

       * value，可以规定input元素中的值

         **PS：**在文本框中会显示为灰体字的提示文字，在其他的属性中无法显示，是为了给后台人员   做区分的

       * checked，在单选按钮和复选按钮中可以使用，效果是当页面打开时，添加checked的按钮将会被默认选中。

       * maxlength，用来规定字段中可输入字符的最大长度，超过maxlength的将无法输入。

       * type包含属性：

         1. text，文本框，可以输入文字，默认宽度为20个字符。

         2. password,输入密码用的，输入的东西将被用符号代替，无法被正常看见。

         3. radio，单选框，多选一，需要用将`<input type="radio">`放在每一个选项后面，需要在name属性中填入相同的名字，否则，如果名字不同或者名字属性漏写，则会发生可以多选的错误。

         4. checkbox，多选框，和radio的使用方法相似。

         5. submit，提交按钮，此按钮点击后将表单域中的值提交到服务器中

            可以通过在input元素中加入value元素来改变默认显示的文字“提交”

         6. reset，重置按钮，可以将表单中值值全部重置。

            同样可以通过在input元素中加入value元素来改变默认显示的文字“重置”

         7. button，是一个普通按钮（与未来将要学习的js有联系

         8. file，可以使用户可以通过这个按钮完成为文件的上传

         **特殊:** label标签，不属于input但是经常和input同时使用，因此在这里记录。

         可以将提示信息和input输入表单元素关联在一起，即点击提示信息即可将光标自动移到对应的表单元素里面。

         **语法有点难，重点记忆:**

         `<label for="填入对应的 input元素的id">`提示信息`</label>`

         `<input type="一般单选框和多选框常用这个" name="最好和id一样" id="比较直观的id"/>`

     * **select下拉表单元素**

       是一个类似于菜单点击后展开的方式

       语法方式：

       提示信息

       `<select>`

       `<option>`选项`</option>`

       `<option>`选项`</option>`

       ......

       `</select>`

       **PS:** 要至少包含一个选项，可以在option中定义selected="selected"，即当前选项为默认的选项。

     * **textarea文本域元素**

       可以收纳多行文本输入，相当于一个大的text。可以通过属性设置它的行数和最大字符数（但是一般实际开发中都用css来完成设置）

       **注意：**这个文本域元素会有空格的显示

   * **提示信息（解释说明表单控件（元素）的具体用处）**



### 四、HTML之块级元素

**特点:** 在块级元素中，只有属于此块级元素的元素可以存在，其他的元素都无法和块级元素同行，可以看作一个盒子。

虽然可以通过css的修饰控制它的宽度和高度，但是即使它的宽度被限制在无法占满一行，依然没有元素可以和它在一行内共存。

类似`<p>``<h>`这样可以容纳文字类的块级元素无法在内部容纳<div>标签，即使强制将代码嵌套输入，在网页生成时，查看网页的源代码依然会发现<div>不在<p>标签中。



### 五、HTML之行内元素

**特点:** 行内元素又被称作内联元素或内嵌元素，和块级元素相比，它可以和其它元素和平共处于一行。

如果不使用`<br />`这种分行标签的话，即使在代码中分段，在网页中也会自动回到一行。

与块级元素相反，行内元素无法设置它的宽度和高度。

**PS:** 后期可以使用css的display属性转换块级元素行内元素以及行内块元素

**行内块元素:** 有行内元素的特点（元素可以全在一行），也有块级元素的特点（可以为元素设置高度）



---



# 参考资料

- [菜鸟教程之HTML 教程- (HTML5 标准)](https://www.runoob.com/html/html-tutorial.html)

